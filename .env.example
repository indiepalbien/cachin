# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-change-this-in-production
DEBUG=False
USE_POSTGRES=False

# PostgreSQL Configuration (for local development)
PGDATABASE=railway
PGUSER=postgres
PGPASSWORD=
PGHOST=localhost
PGPORT=5432

# Email Fetch (IMAP) Configuration
EMAIL_FETCH_IMAP_HOST=imap.cachinapp.com
EMAIL_FETCH_IMAP_PORT=993
EMAIL_FETCH_IMAP_SSL=True
# Shared mailbox credentials (optional if passing via CLI)
EMAIL_FETCH_USER=automation@cachinapp.com
EMAIL_FETCH_PASS=

# Celery / Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
CELERY_TIMEZONE=UTC
# Set True to run tasks synchronously (no broker) in local dev
CELERY_TASK_ALWAYS_EAGER=False
